<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title>Fortune Master</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.0.0/animate.min.css"/>
        <script type="text/javascript">
        window.onload = function(){
            a = document.getElementById("adminlink");
            a.onclick = function()
            {
                adminmodal = document.getElementById("admodal");
                adminmodal.classList.add("is-active");
            }
            cancelB = document.getElementById("cancelButton");
            cancelB.onclick = function()
            {
                adminmodal = document.getElementById("admodal");
                adminmodal.classList.remove("is-active");
            }
        }
        </script>
    </head>
    <body>
        <div class="hero is-fullheight is-dark is-bold">
            <div class="hero-body">
                <div class="container has-text-centered">
                    <form>
                    <p class="title is-1 is-size-3-mobile is-size-1-desktop">Fortune Master</p>
                    <p class="subtitle is-3 is-size-3-mobile is-size-1-desktop">What's your fortune?</p>
                    <% if(firstTime==0 && editMode==0){ %>
                    <h1 class="title has-text-primary is-size-1 animate__animated animate__fadeIn is-size-3-mobile is-size-1-desktop" name="fortune"><%=randFortune %></h1>
                    <% } %>
                    <div class="buttons is-centered">
                            <button type="submit" class="button is-success is-medium" formaction="/getfortune">Click for Fortune</button>
                            <button type="submit" class="button is-info is-medium" formaction="/changefortune">Change Fortune</button>
                    </div>
                    </form>
                </div>
                <div>
                    <figure class="image is-hidden-mobile">
                        <img src="images/fairy.png">
                    </figure>
                </div>
            </div>
            <div class="hero-foot">
                <nav class="tabs">
                    <div class="container">
                      <ul>
                        <li><a href="#" id="adminlink">Admin</a></li>
                      </ul>
                    </div>
                  </nav>
            </div>
        </div>
        <div class="modal" id="admodal">
            <form>
                <div class="modal-background"></div>
                <div class="modal-card">
                    <header class="modal-card-head">
                    <p class="modal-card-title">Login to Continue</p>
                    <button class="delete" aria-label="close"></button>
                    </header>
                    <section class="modal-card-body">
                            <div class="field">
                                <div class="control">
                                    <input type="text" class="input is-primary" placeholder="Username" name="txtUname">
                                </div>
                            </div>
                            <div class="field">
                                <div class="control">
                                    <input type="password" class="input is-primary" placeholder="Password" name="txtPword">
                                </div>
                            </div>
                    </section>
                    <footer class="modal-card-foot">
                    <button class="button is-success" formaction="/adminpage">Login</button>
                    <button class="button" id="cancelButton">Cancel</button>
                    </footer>
                </div>
            </form>
        </div>
    </body>
</html>